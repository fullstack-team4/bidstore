<% if user.bids.any? %>
<h1>Your bids</h1>
  <% @bids.each do |bid| %>
    <p>you bid <%= number_to_currency(bid.amount) %> for <%= link_to bid.product.title, product_path(bid.product) %></p>
  <% end %>
<% else  %>
<h3>No bids yet! Go bid!</h3>
<% end %>
